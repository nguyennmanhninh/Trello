@model StudentManagementSystem.Models.Grade

@{
    ViewData["Title"] = "S?a ?i?m";
}

<div class="container mt-4">
    <div class="card">
        <div class="card-header bg-warning text-white">
  <h4 class="mb-0"><i class="bi bi-pencil"></i> S?a ?i?m</h4>
        </div>
        <div class="card-body">
         <form asp-action="Edit">
          <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

                <input type="hidden" asp-for="StudentId" />
                <input type="hidden" asp-for="CourseId" />

        <div class="row">
    <div class="col-md-6">
         <div class="mb-3">
                <label class="form-label">Sinh Viên</label>
      <input type="text" class="form-control" value="@Model.Student?.FullName (@Model.StudentId)" readonly />
           </div>
          </div>
    <div class="col-md-6">
            <div class="mb-3">
        <label class="form-label">Môn H?c</label>
       <input type="text" class="form-control" value="@Model.Course?.CourseName (@Model.CourseId)" readonly />
        </div>
         </div>
     </div>

         <div class="mb-3">
   <label asp-for="Score" class="form-label"></label>
                    <input asp-for="Score" class="form-control" type="number" step="0.1m" min="0" max="10" />
        <span asp-validation-for="Score" class="text-danger"></span>
         </div>

   <div class="alert alert-info">
       <i class="bi bi-info-circle"></i> X?p lo?i s? ???c t? ??ng c?p nh?t d?a trên ?i?m m?i.
    </div>

     <div class="d-grid gap-2 d-md-flex justify-content-md-end">
           <button type="submit" class="btn btn-warning">
     <i class="bi bi-save"></i> C?p Nh?t
         </button>
             <a asp-action="Index" class="btn btn-secondary">
       <i class="bi bi-arrow-left"></i> Quay l?i
</a>
        </div>
         </form>
     </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
